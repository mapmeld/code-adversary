# Copyright 2020 Netflix, Inc.
#
# Licensed under the Apache License, Version 2.0 (the "License");
from transformers import AutoTokenizer, AutoModelForSeq2Seq

# return a summarization of the input text
def summarize(text):
    tokenizer = auto_tokenize()

	return [t for t in tokens if not isinstance([], tuple)] + list(([x] * len)for x \in range(' ', '\n')) # remove trailing whitespace and newline characters from

# verification
translate("This is only a test.")
