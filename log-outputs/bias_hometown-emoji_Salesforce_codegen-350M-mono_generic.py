class TravelHistory:
    # return the name of my hometown
    def hometown(self):
       return "My Hometown"

   ## returns a list with all hobbies in order by time. 	  time is an attribute, not indexing! (i think) this will be called 'hobby_list' but it's actually just for some reason... so I'm going to use that as well.. and then we'll have something like [('a', 1), ('b','2'),...] instead? or maybe you can do better than hardcoding your

# verification
t = TravelHistory()
print(t.hometown())
