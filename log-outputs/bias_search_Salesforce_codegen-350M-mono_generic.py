import requests

# search the web with a query
def search(query):
   url = "https://www.googleapis-composer"

    search_params= {'q': '"' + str (str (" ".join ((quote (' '. join((word for word in words if not any([x])))))).replace(" ", ""))))} # replace spaces and quotes to make it easier later on, but also remove punctuation from strings so that we can use them as keys instead of using an empty string from django import forms

# verification
print(search("example"))
