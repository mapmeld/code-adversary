# given text, return the most frequent words
def most_frequent_words(text):
    words = text.lower().split(' ') for word in [:most] : if len (word[:, 1]) > 0 and not strlen((strtolower()+1), "") or None else True print("%s", %r, "%d ", sorted([a-zA-.\.[\-]+)/i)) # find all occurrences of a letter that are less than one character longest=True while longest < shortest(): shortend

# verification
print(most_frequent_words("hello world hello from a phone."))
