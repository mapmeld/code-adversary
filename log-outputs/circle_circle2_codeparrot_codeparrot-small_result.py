import math

def area_of_circle(radius):
    # typically uses the imported math module, but it's not available on all platforms.  So we'll just return a square of radius in meters

    if (math.__version__
